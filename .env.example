# Public hostname clients reach this server on.
PUBLIC_HOSTNAME=turn.example.com
# Public IP address of this machine, handed out by coturn as its relay address.
PUBLIC_IP=203.0.113.10
# TURN credentials. Must match [relay] in config/peerConfig.toml. Generate your own.
TURN_USER=changeme
TURN_PASSWORD=changeme

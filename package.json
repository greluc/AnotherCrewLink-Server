{
	"name": "acl-server-testclient",
	"version": "0.0.0",
	"private": true,
	"description": "Not the server. The reference Socket.IO client that tests/wire.rs drives against the Rust server, so the wire format is checked against the implementation the shipping clients use rather than against another copy of our own assumptions.",
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"socket.io-client": "^4.8.3"
	}
}
